name: commit-signed
on:
  workflow_dispatch:
  workflow_call:
jobs:
  update:
    runs-on: composite
    permissions: write-all
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - run: pwd && ls -lsa && ls -lsa .github
