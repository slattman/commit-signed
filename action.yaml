name: Commit Signed
description: a githut workflow action designed to commit signed with the verified github-actions[bot]
branding:
  icon: archive
  color: gray-dark
runs:
  using: composite
  steps:
    - run: curl https://raw.githubusercontent.com/slattman/commit-signed/refs/tags/latest/.github/api/createCommitOnBranch.sh | sh -
      shell: bash
